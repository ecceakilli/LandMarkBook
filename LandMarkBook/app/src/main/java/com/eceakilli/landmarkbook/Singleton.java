package com.eceakilli.landmarkbook;
/*

//Singleton getIntance nerde cagırsam cagırayım hep aynı objeyi döndürecek
public class Singleton {

   private LandMark sentLandmark;
   private static Singleton singleton;


   private Singleton(){

   }
   private Singleton(LandMark sentLandmark){

    }

    public LandMark getSentLandmark() {
        return sentLandmark;
    }

    public void setSentLandmark(LandMark sentLandmark) {
        this.sentLandmark = sentLandmark;
    }
    public static  Singleton getInstance(){
        if (singleton == null){
            singleton=new Singleton();
        }
        return singleton;
    }
}
*/